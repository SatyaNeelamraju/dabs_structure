# This is a Databricks asset bundle definition for dabs_demo.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dabs_demo
  uuid: 1dfae92b-5998-4cd8-a0ce-d5312b33906e

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-2a940dee-6edf.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-2a940dee-6edf.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/satya.neelamraju@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/satya.neelamraju@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: satya.neelamraju@gmail.com
        level: CAN_MANAGE
